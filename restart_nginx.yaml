---
- name: Restart Nginx on AWS EC2 instances
  hosts: tag_OS_ubuntu
  user: ubuntu
  become: true
  tasks:
    - name: Stop Nginx service
      ansible.builtin.systemd:
        name: nginx
        state: stopped
