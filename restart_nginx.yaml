---
- name: Restart Nginx on AWS EC2 instances
  hosts: ip-address
  user: ubuntu
  become: true
  tasks:
    - name: Start Nginx service
      ansible.builtin.systemd:
        name: nginx
        state: started
